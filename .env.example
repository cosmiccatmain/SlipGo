# Leave both unset to run on the seeded sample data in src/data/.
#
# Set both to read the live tables over PostgREST. Use the publishable/anon
# key only — row-level security is what protects neighbor profiles, so a
# service-role key here would hand every private profile to the browser.
SUPABASE_URL=
SUPABASE_ANON_KEY=
